# example-spin
example spin

```sh
$ spin new http-rust hello-rust
$ spin build
$ spin up  # run localhost
$ curl -i http://localhost:3000/hello
```


# with Fermyon
```sh
$ spin login
$ spin deploy
```
